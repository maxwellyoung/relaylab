{
  "name": "relaylab",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "downstream",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n coordinator,downstream,client -c blue,magenta,green \"npm run dev -w server\" \"npm run dev -w downstream\" \"npm run dev -w client\"",
    "start": "node scripts/start-production.mjs",
    "test": "npm run test --workspaces --if-present",
    "test:contract": "npm run test -w server -- public-contract.test.ts",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "smoke": "node scripts/smoke-production.mjs",
    "verify": "npm test && npm run typecheck && npm run build && npm run smoke && npm audit --omit=dev --audit-level=high"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
